A XULRunner-based approach to Zotero server side translation.

Currently supports import, export, and web translation. Modify the paths
at the top of build.sh to build. (You will need a Zotero extension
checkout and a copy of XULRunner.)