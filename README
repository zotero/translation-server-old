An xpcshell-based approach to Zotero server side translation.

Currently supports import, export, and web translation. Modify the paths
at the top of build.sh and the parameters in config.js to build. (You will need
a copy of the XULRunner SDK.)